import React from "react";
import { AbsoluteFill } from "remotion";

export const Main: React.FC = () => {
  return (
    <AbsoluteFill style={{ backgroundColor: "#000" }}>
      {/* Replace with compositions from zm-motion, e.g. IconOrbitScene, ZoomMeetingScene */}
    </AbsoluteFill>
  );
};
